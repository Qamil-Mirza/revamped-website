# Temporary: unblock installs past the next-themes@0.3.0 (peer React <=18) vs
# React 19 conflict so Vercel/CI `npm install` doesn't fail. Remove this once
# next-themes is upgraded to a React 19-compatible version. See issue #6.
legacy-peer-deps=true
